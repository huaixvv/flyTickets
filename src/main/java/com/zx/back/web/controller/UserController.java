package com.zx.back.web.controller;import com.alibaba.druid.pool.vendor.SybaseExceptionSorter;import com.zx.back.model.User;import com.zx.back.service.IUserService;import com.zx.back.service.UserServiceImpl;import com.zx.back.utils.RandomId;import org.springframework.beans.factory.annotation.Autowired;import org.springframework.stereotype.Controller;import org.springframework.ui.Model;import org.springframework.web.bind.annotation.RequestMapping;import javax.servlet.http.HttpSession;@Controller@RequestMapping("user")public class UserController {    @Autowired    private IUserService userService;    @RequestMapping("login")    public String login(String username, String password, HttpSession session){        User user = userService.findByName(username);        //System.out.println(user);        if (user.getUname().equals(username)&&user.getPsd().equals(password)){            session.setAttribute("user",user);            return "flysearch";        }       return null;    }    @RequestMapping("regist")    public String regist(String uname, String psd, int age, String address, String sex){        RandomId randomId = new RandomId();        String uid = "user"+randomId.generateShortUuid();        User user = new User();        user.setAdress(address);        user.setSex(sex);        user.setAge(age);        user.setPsd(psd);        user.setUname(uname);        user.setUid(uid);        userService.savaOrUpdate(user);        return "login";    }}